Many todo's

* Refactor to support multiple robots
* Complete test coverage
* Add support for placement of walls (obstructions other then boundary edges)
* Extend Robot definition to support naming them
* Extend commands to be able to send commands to specific robot
* Create Web API interface and move simulator and board etc behind it (support multiple users)
* Replace console with GUI (WPF / MVC / HTML5 etc) that supports control via UI in addition to console commands (or instead of)
* Add replay funcitonality
* Add AI to robots
* Dependency Inject the logger using .net Core's ILogger interface
* Language localization
* Moving of magic information strings to resource file (could be done as part of language localization)
* Addition of message bus (RabbitMQ ?) to handle command requests
* List of possibilities goes on ... :-)
